import { ValidatorApp } from "@/components/u-estate/validator";

export const metadata = {
  title: "u-estate · Painel de Validadores",
};

export default function ValidatorPage() {
  return <ValidatorApp />;
}
